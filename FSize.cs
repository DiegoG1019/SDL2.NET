namespace SDL2.NET;

public struct FSize
{
    public float Width { get; }
    public float Height { get; }

    public FSize(float width, float height)
    {
        Width = width;
        Height = height;
    }
}